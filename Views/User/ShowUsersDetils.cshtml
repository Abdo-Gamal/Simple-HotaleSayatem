@using HotalManagmentSystem.Models
@model List<HotalManagmentSystem.Models.User>
@{
    ViewBag.Title = "ShowUsersDetils";
}

<br>
<div class="container wow fadeInUp">
    <div class="row">
        <div class="col-md-12">
            <h3 class="section-title">Users Details</h3>
            <div class="section-title-divider"></div>
        </div>
    </div>
</div>

<br />

<!--<html>
<head>
    <!-- this meta for coding -->
    <!--<meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
    <style>
        table, th, td {
            border: 1px solid black;
            /*border-collapse: collapse;*/
        }

        table {
            border-spacing: 5px;
        }
    </style>
</head>
<!--<bode>
    <table class="table table-dark table-sm" style="width: 120%;">-->
        <!--@*<th>UserId</th>*@
        <th>UserName</th>
        <th>UserNationalID</th>
        <th>UserPhone</th>
        <th>UserBarthDate</th>
        <th>DetailsAboutOneUser</th>
        <th>Edit Data</th>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.UserName</td>
                <td>@item.UserNationalID</td>
                <td>@item.UserPhone</td>
                <td>@item.UserBarthDate.ToString("dd-MM-yyyy")</td>
                <td><p><a href="/User/DetailsAboutOneUser/@item.UserID" class="btn btn-success">Go To Show Details One User</a></p></td>
                <td><p><a href="/User/Edit/@item.UserID" class="btn btn-success">Edit</a></p></td>

            </tr>
        }
    </table>
</bode>
</html>-->

<table class="table">
    <thead class="table-dark">
        <tr>
            <th>UserName</th>
            <th>UserNationalID</th>
            <th>UserPhone</th>
            <th>UserBarthDate</th>
            <th>DetailsAboutOneUser</th>
            <th>Edit Data</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            @foreach (var item in Model)
            {
            <tr>
                <td>@item.UserName</td>
                <td>@item.UserNationalID</td>
                <td>@item.UserPhone</td>
                <td>@item.UserBarthDate.ToString("dd-MM-yyyy")</td>
                <td><p><a href="/User/DetailsAboutOneUser/@item.UserID" class="btn btn-dark">Go To Show Details One User</a></p></td>
                <td><p><a href="/User/Edit/@item.UserID" class="btn btn-dark">Edit</a></p></td>

            </tr>
        }
            </tr>
        </tbody>
    </table>   