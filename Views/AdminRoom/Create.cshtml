
@using HotalManagmentSystem.Models;
@using HotalSystem.ViewModel;
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@model Room
<h2> add new Room</h2>

@using (Html.BeginForm("Create", "AdminRoom", FormMethod.Post)) ///
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">

    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <div class="form-group">
        @Html.LabelFor(model => model.IsReseved, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            <input  name="IsReseved" value="false"  > </input>
            @Html.ValidationMessageFor(model => model.IsReseved, "", new { @class = "text-danger" })
        </div>
    </div>

    @*<div class="form-group">
            @Html.LabelFor(model => model.FK_CategoryID, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownListFor(model => model.FK_CategoryID,new SelectList{ Model.RoomCategorys },new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.FK_CategoryID, "", new { @class = "text-danger" })
            </div>
        </div>*@
    <label for=""> Room Category</label>
    <div>
        <select name="FK_CategoryID" id="FK_CategoryID" class="form-control ">
            <br>
            @foreach (RoomCategory Cat in ViewBag.Cats)
            {

                <option value="@Cat.CategoryID" selected>@Cat.CategoryNumberOfPerson</option>

            }
        </select>
    </div>

    <br>
    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="Create" class="btn btn-primary" />
        </div>
    </div>
</div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
