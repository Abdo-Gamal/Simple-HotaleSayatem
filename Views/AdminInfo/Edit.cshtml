@model HotalManagmentSystem.Models.Admin
@using HotalManagmentSystem.Models

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<h2>Edit Admin</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Admin_ID)

        <div class="form-group">
            @Html.LabelFor(model => model.AdminEmail, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.AdminEmail, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.AdminEmail, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.AdminPassward, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.AdminPassward, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.AdminPassward, "", new { @class = "text-danger" })
            </div>
        </div>

        @*<div class="form-group">
                @Html.LabelFor(model => model.emb_ID, htmlAttributes: new { @class = "control-label col-md-2" })
                <div class="col-md-10">
                    @Html.EditorFor(model => model.emb_ID, new { htmlAttributes = new { @class = "form-control" } })
                    @Html.ValidationMessageFor(model => model.emb_ID, "", new { @class = "text-danger" })
                </div>
            </div>*@
        <!---->
        <label for=""> Admin Name</label>
        <div>
            <select name="emb_ID" id="emb_ID" class="form-control form-control">
                @foreach (Employee emp in ViewBag.Emps)
                {
                    if (emp.Employee_ID == Model.emb_ID)
                    {
                        <option value="@emp.Employee_ID" selected>@emp.EmployeeName</option>
                    }
                    else
                    {
                        <option value="@emp.Employee_ID">@emp.EmployeeName</option>
                    }
                }
            </select>

        </div>
        <!---->
        @if (ViewBag.Message != null)
        {
            var cssview = (ViewBag.Success == true ? "alert-success" : "alert-danger");
            <div class="@cssview ">
               <h3>@ViewBag.Message</h3> 
            </div>
         }
        <!---->
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
